vrrp_script chk_haproxy {
    script "killall -0 haproxy"
    interval 2
}
 
vrrp_instance VI_1 {
    state BACKUP
    interface {{ keepalived_check_interface }}
    virtual_router_id 51
    priority 100
    advert_int 1
    authentication {
        auth_type PASS
        auth_pass GGhdN3L13l
    }
    virtual_ipaddress {
        {{ keepalived_virtual_ipaddr }} dev {{ keepalived_check_interface }}
    }
    track_script {
        chk_haproxy
    }
}
